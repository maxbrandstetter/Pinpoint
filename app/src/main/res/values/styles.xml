<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#80CED7</item>
        <item name="android:textColor">#26686D</item>
        <item name="colorAccent">#CCDBDC</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="Theme.Design.NoActionBar" />

    <style name="AppTheme.PopupOverlay" parent="Theme.AppCompat.Light" />

</resources>
